# Copy to .env and adjust as needed.

# A Postgres connection string — a free Prisma Postgres database
# (console.prisma.io) works well for local dev too. Use the *direct*
# connection string, not the pooled/Accelerate one — see README.md.
DATABASE_URL="postgres://user:password@host:5432/postgres?sslmode=require"
