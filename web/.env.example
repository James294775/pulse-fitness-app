# Copy to .env and adjust as needed.

# Local dev: SQLite file, relative to the `web/` directory.
# Deploying: point this at a hosted Postgres/libSQL instance instead — see
# README.md "Deploying" for why SQLite can't be used as-is on Vercel.
DATABASE_URL="file:./dev.db"
